{
    "name": "koteyka",
    "private": true,
    "scripts": {
        "build": "weboops --mode build -o ./dist/ --publicPath /koteyka/",
        "dev": "weboops --mode development"
    },
    "devDependencies": {
        "@weboops/core": "^0.2.8",
        "gh-pages": "^3.2.3"
    }
}
